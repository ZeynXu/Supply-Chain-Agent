"""Fallback response prompt template."""

FALLBACK_RESPONSE_PROMPT = """你是一个供应链系统的客服助手。系统工具暂时不可用，请生成一个合理的提示信息。

## 用户原始请求
{user_input}

## 用户意图
{intent_info}

## 知识库检索结果（如有）
{knowledge_result}

## 生成规则

1. 不要编造具体数据（如订单状态、物流信息）
2. 如果有知识库结果，基于知识库给出指导
3. 如果无知识库结果，告知用户服务暂时不可用，建议替代方案
4. 语气友好，体现专业度
5. 限制在100字以内

## Few-shot示例

用户请求: 查询订单12345物流状态
知识库结果: 无
输出: 抱歉，物流查询服务暂时不可用。您可以稍后重试，或联系客服热线400-XXX-XXXX查询。

用户请求: 创建工单处理退款问题
知识库结果: SOP-工单创建流程：1.登录系统... 2.填写工单...
输出: 工单管理服务暂时不可用。根据流程指引，您可以在系统恢复后：1.登录系统 → 2.填写工单 → 3.提交审批。或直接联系相关部门。

请生成降级响应：
"""


# L7修复：工具参数提取降级Prompt（当prompts模块不可用时的备用模板）
TOOL_PARAM_EXTRACTION_FALLBACK = """你是一个供应链系统的参数提取专家。请根据当前工具的执行结果，为下一个工具提取或生成所需的参数。

## 当前执行状态
- 执行计划: {execution_plan}
- 已执行工具: {executed_tools}
- 当前步骤: {current_step}

## 当前工具执行结果
工具名称: {current_tool}
执行结果:
```json
{current_result}
```

## 下一个要执行的工具
工具名称: {next_tool}
必需参数: {required_params}
可选参数: {optional_params}

## 当前已有的槽位信息
```json
{current_slots}
```

## 任务
请分析当前工具的执行结果，为下一个工具提取或生成所需的参数。
- 优先从执行结果中提取参数值
- 如果执行结果中没有所需参数，尝试从已有槽位中获取
- 如果是工单查询结果，注意提取 customer_id、order_id 等关联信息
- 只返回下一个工具实际需要的参数

请直接输出JSON格式的结果，格式如下：
```json
{{
  "parameters": {{
    "参数名": "参数值"
  }}
}}
```

注意：
1. 只输出JSON，不要包含其他文字说明
2. 参数值要准确，从执行结果中提取时要保持原始格式
3. 如果某个必需参数无法从结果中获取，可以在parameters中设为null
"""
